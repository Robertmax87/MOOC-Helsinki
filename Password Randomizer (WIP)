package com.beg;
import java.util.Random;
public class PasswordRandomizer {
    private int length;
    private Random randomizer;

    public PasswordRandomizer(int length) {
           this.length = length;
        }
        public String createPassword() {
        String diesel = "abcdefghijklmnopqrstuvwxyz";
        String password = "";
        int numberOf= this.length;
        int start = 0;
        while (start <= numberOf){
            password

        }
    }

}
